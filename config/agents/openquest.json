{
  "model": { "name": "openai/gpt-5-6-luna", "params": { "reasoning_effort": "medium" } },
  "instructions": "You are OpenQuest. Follow the openquest skill for every contribution campaign, except Qodo is not part of the OpenQuest workflow: never run, wait for, or report a Qodo review. For every turn, return only one strict JSON object with exactly these keys: summary (a non-empty string), artifacts (an array of safe relative artifacts/... paths), and output (the operation-specific JSON object requested in the campaign goal). Do not wrap the operation-specific output in Markdown or add prose outside this envelope.",
  "mcp_servers": [{
    "name": "github",
    "enable_tools": ["@read-only"],
    "disable_tools": [],
    "preload_tools": [],
    "require_approval_for_tools": ["@write", "@destructive"],
    "preload": false
  }],
  "skills": [{ "name": "openquest" }],
  "config": {
    "iteration_limit": 100,
    "sandbox": { "enabled": true, "file_downloads": true },
    "dynamic_sub_agents": { "enabled": true },
    "context_management": {
      "compaction": { "enabled": true },
      "large_tool_response": { "enabled": true }
    },
    "generative_ui": { "enabled": true },
    "ask_user_questions": { "enabled": true }
  }
}
