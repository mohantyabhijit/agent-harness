{
  "name": "incident-forge",
  "version": "0.1.0",
  "private": true,
  "description": "Human-in-the-loop production incident investigator built with TrueForge",
  "scripts": {
    "dev": "node src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test",
    "lint": "node --check src/domain.js && node --check src/cli.mjs && node --check src/server.mjs"
  },
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@truefoundry/trueforge": "0.1.4"
  }
}
